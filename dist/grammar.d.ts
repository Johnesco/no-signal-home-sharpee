/**
 * No Signal Home — Grammar Extensions
 *
 * Custom vocabulary and grammar patterns for story-specific actions.
 */
import type { Parser } from '@sharpee/parser-en-us';
export declare function extendParser(parser: Parser): void;

/**
 * Shared logic for locking and unlocking actions
 */
import { ActionContext, ValidationResult } from '../enhanced-types';
import { IFEntity } from "../../../world-model/index";
import { ISemanticEvent } from "../../../core/index";
/**
 * Shared message constants for lock/unlock validation
 * These are message IDs, not English strings - lang-en-us resolves them
 */
export declare const LOCK_MESSAGES: {
    NO_KEY: string;
    KEY_NOT_HELD: string;
    WRONG_KEY: string;
};
export interface LockAnalysis {
    target: IFEntity;
    key?: IFEntity;
    isContainer: boolean;
    isDoor: boolean;
    requiresKey: boolean;
    keyHeld: boolean;
}
/**
 * Analyzes the lock/unlock context to determine object types and key requirements
 */
export declare function analyzeLockContext(context: ActionContext, target: IFEntity, key?: IFEntity): LockAnalysis;
/**
 * Validates key requirements for locking/unlocking
 * Returns validation result with appropriate error if validation fails
 */
export declare function validateKeyRequirements(context: ActionContext, target: IFEntity, key: IFEntity | undefined, isLocking: boolean): ValidationResult | null;
/**
 * Creates error events based on lock/unlock result
 */
export declare function createLockErrorEvent(context: ActionContext, result: any, target: IFEntity, key: IFEntity | undefined, isLocking: boolean): ISemanticEvent | null;
/**
 * Determines the appropriate message based on lock/unlock action
 */
export declare function determineLockMessage(isLocking: boolean, hasKey: boolean): string;
//# sourceMappingURL=lock-shared.d.ts.map
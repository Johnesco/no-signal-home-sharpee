/**
 * Shared helpers for wearing and taking_off actions
 *
 * Centralizes common logic for wearable item manipulation including:
 * - Layering conflict detection
 * - Event data building
 * - Wearable validation helpers
 */
import { ActionContext } from '../enhanced-types';
import { ISemanticEvent } from "../../../core/index";
import { IFEntity, WearableTrait } from "../../../world-model/index";
/**
 * Context analysis for wearable actions
 */
export interface WearableContext {
    item: IFEntity;
    actor: IFEntity;
    wearableTrait: WearableTrait;
    inventory: IFEntity[];
}
/**
 * Analyzes the wearable context for an action
 */
export declare function analyzeWearableContext(context: ActionContext, item: IFEntity, actor: IFEntity): WearableContext;
/**
 * Checks for layering conflicts when wearing an item
 * Returns conflicting item if found, null otherwise
 */
export declare function checkWearingConflicts(wearableContext: WearableContext): IFEntity | null;
/**
 * Checks for items blocking removal due to layering
 * Returns blocking item if found, null otherwise
 */
export declare function checkRemovalBlockers(wearableContext: WearableContext): IFEntity | null;
/**
 * Builds event parameters including body part and layer info
 */
export declare function buildWearableEventParams(item: IFEntity, wearableTrait: WearableTrait): Record<string, unknown>;
/**
 * Creates a blocked event for wearable actions
 *
 * Uses domain event pattern (ADR-097): event carries messageId directly.
 */
export declare function createWearableBlockedEvent(context: ActionContext, messageId: string, reason: string, params?: Record<string, unknown>): ISemanticEvent;
/**
 * Creates a success event for wearable actions
 *
 * Uses domain event pattern (ADR-097): event carries messageId directly.
 */
export declare function createWearableSuccessEvent(context: ActionContext, messageId: string, params: Record<string, unknown>): ISemanticEvent;
/**
 * Checks if an item is currently worn by the actor
 */
export declare function isWornByActor(item: IFEntity, actor: IFEntity): boolean;
/**
 * Checks if an item has special removal restrictions
 */
export declare function hasRemovalRestrictions(wearableTrait: WearableTrait): boolean;
//# sourceMappingURL=wearable-shared.d.ts.map
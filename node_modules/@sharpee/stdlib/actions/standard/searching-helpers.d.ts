/**
 * Shared helpers for searching and examining actions
 *
 * These utilities help with content discovery, concealed item handling,
 * and message determination for sensory actions.
 */
import { IFEntity } from "../../../world-model/index";
import { ActionContext } from '../enhanced-types';
/**
 * Context about what was searched and found
 */
export interface SearchContext {
    target: IFEntity;
    contents: IFEntity[];
    concealedItems: IFEntity[];
    isLocation: boolean;
    targetType: 'container' | 'supporter' | 'location' | 'object';
}
/**
 * Analyze search target and find all contents
 */
export declare function analyzeSearchTarget(context: ActionContext, target?: IFEntity): SearchContext;
/**
 * Reveal concealed items found during search
 */
export declare function revealConcealedItems(items: IFEntity[]): void;
/**
 * Determine the appropriate search message based on what was found
 */
export declare function determineSearchMessage(searchContext: SearchContext): {
    messageId: string;
    params: Record<string, any>;
};
/**
 * Build search event data
 */
export declare function buildSearchEventData(searchContext: SearchContext): Record<string, any>;
//# sourceMappingURL=searching-helpers.d.ts.map
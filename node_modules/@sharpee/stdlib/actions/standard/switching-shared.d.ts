/**
 * Shared logic for switching_on and switching_off actions
 */
import { ActionContext } from '../enhanced-types';
import { IFEntity } from "../../../world-model/index";
export interface SwitchingAnalysis {
    target: IFEntity;
    isLightSource: boolean;
    otherLightsPresent: boolean;
    isInSameRoom: boolean;
    lightRadius?: number;
    lightIntensity?: string;
    willAffectDarkness?: boolean;
}
/**
 * Analyzes the switching context to determine light and room conditions
 */
export declare function analyzeSwitchingContext(context: ActionContext, target: IFEntity): SwitchingAnalysis;
/**
 * Determines the appropriate message based on device type and switching direction
 */
export declare function determineSwitchingMessage(isOn: boolean, analysis: SwitchingAnalysis, hasSound?: string, wasTemporary?: boolean, hadRunningSound?: string, willOpenClose?: boolean): string;
//# sourceMappingURL=switching-shared.d.ts.map
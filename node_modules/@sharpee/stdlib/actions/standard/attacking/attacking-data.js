"use strict";
/**
 * Data builder for attacking action
 *
 * Following atomic event principles - minimal data per event
 */
Object.defineProperty(exports, "__esModule", { value: true });
exports.attackedDataConfig = exports.buildAttackedData = void 0;
const buildAttackedData = (context) => {
    const target = context.command.directObject?.entity;
    // ADR-080: Prefer instrument field, fall back to indirectObject
    const weapon = context.command.instrument?.entity ?? context.command.indirectObject?.entity;
    if (!target) {
        return {
            target: '',
            targetName: 'nothing',
            unarmed: true
        };
    }
    return {
        target: target.id,
        targetName: target.name,
        weapon: weapon?.id,
        weaponName: weapon?.name,
        unarmed: !weapon
    };
};
exports.buildAttackedData = buildAttackedData;
exports.attackedDataConfig = {
    builder: exports.buildAttackedData,
    protectedFields: ['target', 'targetName']
};
//# sourceMappingURL=attacking-data.js.map
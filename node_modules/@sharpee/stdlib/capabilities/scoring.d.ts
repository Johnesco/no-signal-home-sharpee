/**
 * Scoring capability for tracking player score, achievements, and progress
 */
import { ICapabilitySchema } from "../../world-model/index";
/**
 * Scoring capability schema
 * Tracks player score, achievements, and progress
 */
export declare const ScoringCapabilitySchema: ICapabilitySchema;
/**
 * Type-safe scoring data interface
 */
export interface ScoringData {
    scoreValue: number;
    maxScore: number;
    moves?: number;
    achievements?: string[];
    scoreHistory?: Array<{
        action: string;
        points: number;
        timestamp: number;
    }>;
    scoredTreasures?: string[];
}
//# sourceMappingURL=scoring.d.ts.map
/**
 * Command history capability for tracking executed commands
 * Supports the AGAIN/G action functionality
 */
import { ICapabilitySchema } from "../../world-model/index";
/**
 * Command history capability schema
 * Tracks executed commands for replay functionality
 */
export declare const CommandHistoryCapabilitySchema: ICapabilitySchema;
/**
 * Individual command history entry
 */
export interface CommandHistoryEntry {
    actionId: string;
    originalText: string;
    parsedCommand: {
        verb: string;
        directObject?: string;
        indirectObject?: string;
        preposition?: string;
    };
    turnNumber: number;
    timestamp: number;
}
/**
 * Type-safe command history data interface
 */
export interface CommandHistoryData {
    entries: CommandHistoryEntry[];
    maxEntries?: number;
}
//# sourceMappingURL=command-history.d.ts.map
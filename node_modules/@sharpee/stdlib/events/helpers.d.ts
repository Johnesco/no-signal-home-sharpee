/**
 * Helper utilities for common event handler patterns
 */
import { ISemanticEvent } from "../../core/index";
import { IFEntity, WorldModel } from "../../world-model/index";
interface GameEvent extends ISemanticEvent {
    type: string;
    data: Record<string, any>;
}
type EntityEventHandler = (event: GameEvent, world: WorldModel) => void | ISemanticEvent[];
/**
 * Create a handler that toggles a switchable entity
 */
export declare function createToggleHandler(entity: IFEntity): EntityEventHandler;
/**
 * Create a handler that opens something when triggered
 */
export declare function createOpenHandler(entity: IFEntity, target: string): EntityEventHandler;
/**
 * Create a handler that reveals a hidden passage
 */
export declare function createRevealHandler(entity: IFEntity, passage: string): EntityEventHandler;
/**
 * Create a handler that displays a message
 */
export declare function createMessageHandler(message: string, params?: Record<string, any>): EntityEventHandler;
/**
 * Compose multiple handlers into one
 */
export declare function composeHandlers(...handlers: EntityEventHandler[]): EntityEventHandler;
/**
 * Create a handler that only fires once
 */
export declare function createOnceHandler(handler: EntityEventHandler): EntityEventHandler;
/**
 * Create a handler that only fires after N times
 */
export declare function createAfterHandler(count: number, handler: EntityEventHandler): EntityEventHandler;
/**
 * Create a handler with a condition
 */
export declare function createConditionalHandler(condition: (event: GameEvent) => boolean, handler: EntityEventHandler): EntityEventHandler;
export {};
//# sourceMappingURL=helpers.d.ts.map
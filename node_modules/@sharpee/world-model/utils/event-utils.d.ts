/**
 * Event creation utilities for world-model
 *
 * Provides helpers for creating properly-formed SemanticEvents
 */
import { ISemanticEvent } from "../../core/index";
import { EntityId } from "../../core/index";
/**
 * Create a semantic event with world-model specific defaults
 *
 * This wraps the core createEvent to provide consistent event creation
 * across all world-model behaviors.
 */
export declare function createWorldEvent(type: string, payload?: Record<string, unknown>, entities?: {
    actor?: EntityId;
    target?: EntityId;
    instrument?: EntityId;
    location?: EntityId;
    others?: EntityId[];
}): ISemanticEvent;
/**
 * Create an action failed event
 */
export declare function createActionFailedEvent(action: string, reason: string, entities: {
    actor?: EntityId;
    target?: EntityId;
    instrument?: EntityId;
    location?: EntityId;
}, customMessage?: string): ISemanticEvent;
//# sourceMappingURL=event-utils.d.ts.map
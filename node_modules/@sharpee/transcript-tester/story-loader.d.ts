/**
 * Story Loader
 *
 * Dynamically loads and initializes a story for testing.
 */
import { GameEngine, TurnResult } from "../engine/index";
import { ISemanticEvent } from "../core/index";
import { WorldModel } from "../world-model/index";
/**
 * A testable game instance
 */
export interface TestableGame {
    engine: GameEngine;
    world: WorldModel;
    lastOutput: string;
    lastEvents: ISemanticEvent[];
    lastTurnResult: TurnResult | null;
    executeCommand(input: string): Promise<string>;
}
/**
 * Load a story from a path and create a testable game instance
 */
export declare function loadStory(storyPath: string): Promise<TestableGame>;
/**
 * Create a testable game from a story instance
 */
export declare function createTestableGame(story: any): TestableGame;
/**
 * Find all transcript files in a directory
 */
export declare function findTranscripts(dir: string, pattern?: string): string[];
//# sourceMappingURL=story-loader.d.ts.map
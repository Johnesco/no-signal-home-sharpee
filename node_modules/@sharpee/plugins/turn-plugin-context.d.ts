import { EntityId, SeededRandom, ISemanticEvent } from "../core/index";
import { WorldModel } from "../world-model/index";
export interface TurnPluginActionResult {
    actionId: string;
    success: boolean;
    targetId?: EntityId;
    sharedData?: Record<string, unknown>;
}
export interface TurnPluginContext {
    world: WorldModel;
    turn: number;
    playerId: EntityId;
    playerLocation: EntityId;
    random: SeededRandom;
    actionResult?: TurnPluginActionResult;
    actionEvents?: ISemanticEvent[];
}
//# sourceMappingURL=turn-plugin-context.d.ts.map